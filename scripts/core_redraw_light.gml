////redraw_light(light)
/*redraw = true;

/*
with(obj_shadow_caster)
{
    if(collision_rectangle(argument0.x, argument0.y, argument0.x+512, argument0.y+512, argument0, false, false))
    {
        if(!static)
        {
            other.redraw = true;
        }
        other.redraw = true;
    }
}*/

return true;
